import { ADDTOCART, EMAIL, SECURE } from "./constants";

export const widthValue = ({ width, context }) => {
  if (width) {
    return `${width}`;
  }
  switch (context) {
    case ADDTOCART:
    case EMAIL:
    case SECURE:
      return "100%;";
    default:
      return "auto";
  }
};
