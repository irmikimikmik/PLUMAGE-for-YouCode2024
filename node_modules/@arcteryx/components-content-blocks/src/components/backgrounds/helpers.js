export const generateSource = ({ width, height, media, sizes, focalPointX = null, focalPointY = null }) => {
  return {
    width,
    height,
    imgixParams: {
      focalPointX,
      focalPointY,
      width,
      height,
    },
    htmlAttributes: {
      media,
    },
    sizes,
  };
};
