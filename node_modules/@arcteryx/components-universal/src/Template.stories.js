import React from "react";
import { storiesOf } from "@storybook/react";
import { UIHeader, UIFooter, CheckoutHeaderFooter } from "./index";
import { SiteContextProvider } from "@arcteryx/components-contexts";
const stories = storiesOf("Header Footer", module);

const Content = () => (
  <div id="content">
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "50vh",
        textAlign: "center",
      }}
    >
      <div style={{ maxWidth: "50vw" }}>
        The Footer doesn't appear because ui-components footer has code that hides it when loaded within an iFrame. Open
        this storybook in a separate tab by clicking the 2nd icon at the top-right of the screen.
        <br />
        <br />
        Search only seems to load on first page load. If you don't see it after you click into this Storybook component,
        refresh the browser.
      </div>
    </div>
  </div>
);

stories.add("Outdoor", () => (
  <SiteContextProvider market="outdoor" country="ca" language="en">
    <UIHeader />
    <Content />
    <UIFooter />
  </SiteContextProvider>
));

stories.add("LEAF", () => (
  <SiteContextProvider market="leaf" country="ca" language="en">
    <UIHeader />
    <Content />
    <UIFooter />
  </SiteContextProvider>
));

stories.add("Outlet", () => (
  <SiteContextProvider market="sale" country="ca" language="en">
    <UIHeader />
    <Content />
    <UIFooter />
  </SiteContextProvider>
));

stories.add("Veilance", () => (
  <SiteContextProvider market="urban" country="ca" language="en">
    <UIHeader />
    <Content />
    <UIFooter />
  </SiteContextProvider>
));