import React from "react";
import { type TableSizingProps } from "../../molecules/tableSizing/TableSizing";
export interface TableProps {
    category: string;
    disclaimer: string;
    tables: TableSizingProps[];
    title: string;
}
export declare const Table: ({ category, disclaimer, tables, title, }: TableProps) => React.ReactElement;
export default Table;
