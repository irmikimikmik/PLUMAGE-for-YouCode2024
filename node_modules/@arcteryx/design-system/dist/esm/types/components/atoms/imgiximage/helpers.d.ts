export interface ImgixConfig {
    attributeConfig: {
        src: string;
        sizes: string;
        srcSet: string;
    } | undefined;
    commonImgixProps: {
        fit: string;
        crop: string;
        auto: string;
        q: number;
    };
    lqipSource: string;
    imageClassName: string;
}
interface GenerateImgixConfigOptions {
    shouldLazyLoad: boolean;
    shouldCompress: boolean;
    shouldEnhance: boolean;
    quality: number;
    src: string;
}
export declare const generateImgixConfig: ({ shouldLazyLoad, shouldCompress, shouldEnhance, quality, src, }: GenerateImgixConfigOptions) => ImgixConfig;
export {};
