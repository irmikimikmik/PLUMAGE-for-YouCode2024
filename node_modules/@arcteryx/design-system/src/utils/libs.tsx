export const splitLabel = (label: string | undefined): [string, string] => {
  if (typeof label !== "string") {
    throw new Error("Input label must be a string");
  }

  const [firstPart, ...restParts] = label.split(" ");
  const restLabel = restParts.join(" ").trim();

  return [firstPart, restLabel];
};
