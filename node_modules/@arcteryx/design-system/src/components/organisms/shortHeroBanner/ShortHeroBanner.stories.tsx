import { type Meta, type StoryObj } from "@storybook/react";
import { ShortHeroBanner } from "./ShortHeroBanner";
import { shortHeroBannerData } from "./__mocks__/shortHeroBanner.mocks";

const meta: Meta<typeof ShortHeroBanner> = {
  component: ShortHeroBanner,
  title: "Design System/Organisms/ShortHeroBanner",
};
export default meta;

type Story = StoryObj<typeof ShortHeroBanner>;

export const DefaultShortHeroBanner: Story = {
  args: { ...shortHeroBannerData },
};
