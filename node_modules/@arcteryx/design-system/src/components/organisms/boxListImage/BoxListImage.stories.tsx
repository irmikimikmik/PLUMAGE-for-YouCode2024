import { type Meta, type StoryObj } from "@storybook/react";
import { BoxListImage } from "./BoxListImage";
import { boxListImageStoryMock } from "./__mocks__/boxListImageMocks";

const meta: Meta<typeof BoxListImage> = {
  component: BoxListImage,
  title: "Design System/Organisms/BoxListImage",
};

type Story = StoryObj<typeof BoxListImage>;

export const variableSizeBoxListImage: Story = {
  args: boxListImageStoryMock,
};

export default meta;
