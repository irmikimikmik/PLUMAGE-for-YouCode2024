import { type Meta, type StoryObj } from "@storybook/react";
import {
  ButtonModal as ButtonModalComponent,
  type ButtonModalProps,
} from "./ButtonModal";

const args: ButtonModalProps = {
  context: "Standard",
  onClick: () => {},
  label: "Open Modal",
  url: "https://www.arcteryx.com",
};

const meta: Meta<typeof ButtonModalComponent> = {
  component: ButtonModalComponent,
  title: "Design System/Organisms/Button Modal",
  args,
};

export default meta;

type Story = StoryObj<typeof ButtonModalComponent>;

export const ButtonModal: Story = {};
