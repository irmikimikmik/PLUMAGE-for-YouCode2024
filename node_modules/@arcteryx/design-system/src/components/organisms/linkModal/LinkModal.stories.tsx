import { type Meta, type StoryObj } from "@storybook/react";
import { LinkModal as LinkModalComponent } from "./LinkModal";

const args: React.ComponentProps<typeof LinkModalComponent> = {
  text: "Open Modal",
  href: "https://www.arcteryx.com",
};

const meta: Meta<typeof LinkModalComponent> = {
  component: LinkModalComponent,
  title: "Design System/Organisms/Link Modal",
  args,
};

export default meta;

type Story = StoryObj<typeof LinkModalComponent>;

export const LinkModal: Story = {};
