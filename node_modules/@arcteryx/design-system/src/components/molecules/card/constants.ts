interface VariantStyling {
  flexDirectionMobile?: "row" | "column";
  height?: number;
  imageHeight?: number;
  imageWidth?: number;
  maxWidth?: number;
  textAlignment: "left" | "right" | "center";
  width?: number;
}

type VariantStylingMap = Record<
  "product" | "image" | "dynamic" | "video",
  VariantStyling
>;
// Alternatively we could create 4 different components and pass these values into the Card Component.
export const variantStylingMap: VariantStylingMap = {
  product: {
    flexDirectionMobile: "column",
    height: 550,
    imageHeight: 430,
    imageWidth: 120,
    textAlignment: "center",
    width: 180,
  },
  image: {
    flexDirectionMobile: "column",
    textAlignment: "center",
    width: 235,
  },
  dynamic: {
    flexDirectionMobile: "row",
    imageWidth: 400,
    maxWidth: 750,
    textAlignment: "left",
  },
  video: {
    flexDirectionMobile: "column",
    height: 510,
    textAlignment: "center",
    width: 235,
  },
};
