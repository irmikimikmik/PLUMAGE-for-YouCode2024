import { type ButtonProps } from "../../atoms/button/Button.types";
import { type ImageProps } from "../../atoms/image/Image.types";
import { type VideoPlayerProps } from "../../atoms/videoPlayer/VideoPlayer.types";
import { type RichTextBlocks } from "../../molecules/richText/RichText.types";
export interface CardProps {
  button?: ButtonProps;
  content: RichTextBlocks;
  image?: ImageProps;
  shouldOpenModal?: boolean;
  title?: string;
  video?: VideoPlayerProps;
  variant: "product" | "image" | "dynamic" | "video";
}
