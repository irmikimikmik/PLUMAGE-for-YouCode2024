import { type Meta, type StoryObj } from "@storybook/react";
import { TextImage } from "./TextImage";
import { imageBlock } from "./__mocks__/imageBlock";
import { textItem1, textItem2 } from "./__mocks__/textBlocks";

const meta: Meta<typeof TextImage> = {
  component: TextImage,
  title: "Design System/Molecules/TextImage",
};

type Story = StoryObj<typeof TextImage>;

const defaultArgs = {
  image: imageBlock,
  textItems: [textItem1, textItem2],
  displayImageBelowMd: false,
  isImageOnLeft: true,
};

export const SmallImageSize: Story = {
  args: {
    ...defaultArgs,
    imageSize: "small",
  },
};

export const MediumImageSize: Story = {
  args: {
    ...defaultArgs,
    imageSize: "medium",
  },
};

export default meta;
