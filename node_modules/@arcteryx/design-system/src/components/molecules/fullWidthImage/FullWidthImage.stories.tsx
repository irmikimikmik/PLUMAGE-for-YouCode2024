import { type Meta, type StoryObj } from "@storybook/react";
import { FullWidthImage } from "./FullWidthImage";
import { fullWidthImageData } from "./__mocks__/fullWidthImageMocks";

const meta: Meta<typeof Image> = {
  component: FullWidthImage,
  title: "Design System/Molecules/FullWidthImage",
  argTypes: {},
};
export default meta;

type Story = StoryObj<typeof Image>;

const initialProps = fullWidthImageData;

export const DefaultFullWidthImage: Story = {
  args: { ...initialProps },
};
