import type React from "react";
import { Accordion } from "@arcteryx/components-accordion";
import { type Meta, type StoryObj } from "@storybook/react";

type Props = React.ComponentProps<typeof Accordion>;

const meta: Meta<typeof Accordion> = {
  component: Accordion,
  title: "Design System / Molecules / Collapse",
  argTypes: {
    headingLevel: {
      control: "radio",
      options: ["1", "2", "3", "4", "5", "6"],
    },
    isOpenOnPageLoad: { control: "boolean" },
    children: { control: "text" },
  },
};

export default meta;

type Story = StoryObj<typeof Accordion>;
const Args: Props = {
  headingLevel: "4",
  isOpenOnPageLoad: false,
  large: true,
};

export const Collapse: Story = {
  args: {
    ...Args,
    title: "Accordion",
    children: "This is an accordion",
  },
};
