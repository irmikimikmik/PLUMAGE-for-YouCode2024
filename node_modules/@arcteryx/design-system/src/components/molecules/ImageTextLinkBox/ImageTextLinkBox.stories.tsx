import { type Meta, type StoryObj } from "@storybook/react";
import { ImageTextLinkBox as ArcteryxImageTextLinkBox } from "./ImageTextLinkBox"; // Import ImageTextLinkBox component
import { type ImageTextLinkBoxProps } from "./ImageTextLinkBox.types"; // Import type definition

const meta: Meta<typeof ArcteryxImageTextLinkBox> = {
  component: ArcteryxImageTextLinkBox,
  title: "Design System/Molecules/ImageTextLinkBox",
  argTypes: {},
};
export default meta;

type Story = StoryObj<typeof ArcteryxImageTextLinkBox>;

const initialProps: ImageTextLinkBoxProps = {
  image: {
    image: {
      src: "https://images-dynamic-arcteryx.imgix.net/nav/feature/s20/men-shell-finder.jpg",
      alt: "Example Image",
    },
    sourceElementsArray: [],
  },
  label: "Jacket Finder",
  url: "https://arcteryx.com/ca/en/shop/jacket-finder",
  target: "_blank",
};

export const ImageTextLinkBox: Story = {
  args: { ...initialProps },
};
