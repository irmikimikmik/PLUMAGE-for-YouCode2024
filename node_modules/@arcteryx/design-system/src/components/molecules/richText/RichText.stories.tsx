import { type Meta, type StoryObj } from "@storybook/react";
import { RichText } from "./RichText";
import richTextElements from "./__mocks__/richTextElements.json";
import sideBarData from "./__mocks__/sidebarRichText.json";
import { type RichTextBlocks } from "./RichText.types";

const meta: Meta<typeof RichText> = {
  component: RichText,
  title: "Design System/Molecules/RichText",
  argTypes: {},
};

export default meta;

type Story = StoryObj<typeof RichText>;

const defaultStoryBlocks = richTextElements.richText as RichTextBlocks;

export const RichTextEditor: Story = {
  args: { content: defaultStoryBlocks },
};

const sideBarDataBlocks = sideBarData.content;

export const HelpPagesSideBar: Story = {
  args: { content: sideBarDataBlocks as RichTextBlocks },
};
