import { type IconKeys } from "@arcteryx/components-icons";

// We are using the number index type to get the union of all the string literal types in the array.

export type IconName = (typeof IconKeys)[number];

export interface IconProps {
  type: string;
  width: string;
  height: string;
  title: string;
  color: string;
}
