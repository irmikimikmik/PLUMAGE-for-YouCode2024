import { type StoryObj, type Meta } from "@storybook/react";
import { Icon, IconKeys } from "@arcteryx/components-icons";
import { type IconProps } from "./types";

const meta: Meta = {
  component: Icon,
  title: "Design System/Atoms/Icon",
  argTypes: {
    type: {
      options: IconKeys,
      control: { type: "select" },
    },
    width: {
      control: "text",
    },
    height: {
      control: "text",
    },
    title: {
      control: "text",
    },
    color: {
      control: "color",
    },
  },
};
export default meta;

type Story = StoryObj<typeof Icon>;

const playArgs: IconProps = {
  type: "play-button",
  width: "55",
  height: "30",
  title: "play button",
  color: "var(--info-blue)",
};

export const PlayIcon: Story = { args: { ...playArgs } };
