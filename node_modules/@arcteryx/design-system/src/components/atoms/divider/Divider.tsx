import React from "react";
import styled from "styled-components";

const StyledDivider = styled.hr`
  border: 0;
  border-top: 1px solid var(--pinline-grey);
  margin: 1rem;
  width: 100%;
`;

export const Divider: React.FC = () => {
  return <StyledDivider />;
};
