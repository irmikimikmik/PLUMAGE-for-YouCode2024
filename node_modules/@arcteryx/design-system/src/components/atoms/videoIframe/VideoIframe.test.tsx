import React from "react";
import { render } from "@testing-library/react";
import { VideoIframe } from "./VideoIframe";

describe("VideoIframe", () => {
  xit("should render an iframe with the correct props", () => {
    const url = "https://example.com/video.mp4";
    const { getByTestId } = render(<VideoIframe url={url} />);
    const iframe = getByTestId("video-iframe");
    console.log(iframe); // eslint-disable-line no-console
    expect(false).toBe(false);
  });
});
