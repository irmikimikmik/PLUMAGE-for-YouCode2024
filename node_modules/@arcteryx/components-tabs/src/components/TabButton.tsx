import styled from "styled-components";
import React from "react";
import { Breakpoint } from "@arcteryx/components-typography";

interface Prop {
  icon?: JSX.Element;
  children: React.ReactNode;
}

const StyledA = styled.a<{$hasIcon: boolean}>`
  text-decoration: none;
  text-transform: uppercase;
  color: var(--black);

  ${props =>
    props.$hasIcon &&
    `
    display: flex;
    justify-content: center;
    align-items: center;
  `}
`;

const StyledIconWrapper = styled.div`
  margin-right: var(--space-xs);
  margin-bottom: calc(var(--space-xs) * -1);

  display: none;
  ${Breakpoint("md")`
    display: block;
  `}
`;

const TabButton = ({ children, icon }: Prop) => {
  return (
    <StyledA $hasIcon={Boolean(icon)} data-testid="tab-button">
      <>
        {Boolean(icon) && <StyledIconWrapper data-testid="tab-icon">{icon}</StyledIconWrapper>}
        {children}
      </>
    </StyledA>
  );
};

export default TabButton;
